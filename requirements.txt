# 基础工具（必须先安装）
setuptools==80.9.0
wheel>=0.38.0

# 核心依赖
fastapi==0.133.0
uvicorn==0.41.0
python-multipart==0.0.22
Pillow==12.0.0
numpy==2.2.6
opencv-python==4.12.0.88
PyYAML==6.0.1
tqdm==4.67.1
matplotlib==3.10.8
seaborn==0.13.2
pandas==2.3.3

# 使用与本地一致的PyTorch版本（带CUDA但Render会自动用CPU）
torch==2.5.1
torchvision==0.20.1
--extra-index-url https://download.pytorch.org/whl/cpu  # 确保下载CPU版本
